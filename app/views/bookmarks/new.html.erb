<div class='container'>
 <%= simple_form_for [@list, @bookmark] do |f| %>
        <%= f.association :movie %>
        <%= f.input :comment%>
        <%= f.submit class: 'btn btn-primary' %>
      <% end %>
      <%= link_to "See all lists", lists_path %>
</div>
